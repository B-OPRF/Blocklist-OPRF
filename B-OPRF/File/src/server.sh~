#!/bin/bash

start_seconds=$(date +.%6N)

taskset -c 1,2 ./file 1 1234

end_seconds=$(date +.%6N)

execution_time=$(($end_seconds - $start_seconds))
echo "Job execution time:$execution_time nanoseconds"
